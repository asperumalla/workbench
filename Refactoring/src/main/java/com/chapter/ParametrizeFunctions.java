package com.chapter;

/*public class ParametrizeFunctions {

    private String usd(double amount){
        return "$"+amount;
    }

    private String baseCharge (int usage) {
        if (usage < 0) return usd(0);
        var amount =  bottomBand(usage) * 0.03
                    + middleBand(usage) * 0.05
                    + topBand(usage) * 0.07;
        return usd(amount);
    }

    private int bottomBand (int usage) {
        return Math.min(usage, 100);
    }

    private int middleBand (int usage) {
        return usage > 100 ? Math.min(usage, 200) - 100 : 0;
    }

    private int topBand (int usage) {
        return usage > 200 ? usage - 200 : 0;
    }
}*/

